#include <iostream>
#include <vector>

int main(){
    std::vector<std::pair<int, int>> points(3);

    for(int i = 0; i < 3; i++)    {
        std::cin >> points[i].first >> points[i].second;
    }

    std::vector<std::pair<int, int>> xyCnt(4);
    for(int i = 0; i < 3; i++){
        
    }
}